
@using (Html.BeginForm("", "", FormMethod.Post, new { id = "add" }))
{
    <div class="row">
        @*<div class="col-sm-4 form-group">
            <label>ROle Name</label>
            <select id="role"></select>
        </div>*@
        <div class="col-sm-4 form-group">
            <label>User Name</label>
            <select id="user"></select>
        </div>        
    </div>
    <br />
    <div class="row">
        <div class="col-sm-6 form-group">
            <button type="submit" class="btn btn-primary"><i class="fa fa-save"></i> Save</button>
            <button class="btn btn-danger btn-primary" id="modal-cancel" type="button"><i class="fa fa-ban"></i> Cancel</button>
        </div>
    </div>

}

<script type="text/javascript">
    var userData =    @Html.Raw(Json.Encode(ViewBag.Users));
    var roleData =    @Html.Raw(Json.Encode(ViewBag.Roles));
    (function () {

        // $("#role").ejDropDownList({ dataSource: roleData, fields: { text: 'Name', value: 'Id' }, selectedIndex: 0, waterMarkText: "Select", enableFilterSearch: true});
        $("#user").ejDropDownList({ width: '200px', dataSource: userData, fields: { text: 'FullName', value: 'Id' }, selectedIndex: 0, waterMarkText: "Select", enableFilterSearch: true });
        $("#add").submit(function (event) {

            event.preventDefault();
            commonGridEvent.onClickSaveBtn($(this));
        });

        $("#modal-cancel").on('click', function () {
            userRoleAddModal.close();
        });

    })();
</script>